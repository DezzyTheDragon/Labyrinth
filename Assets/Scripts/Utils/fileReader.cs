using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.InputSystem;

/*
public struct ActionList {
    public InputActionReference interactAction;
    public InputActionReference escapeAction;
    public InputActionReference shootAction;
    public InputActionReference healAction;
    public InputActionReference primaryAction;
    public InputActionReference secondaryAction;
}
*/

public class fileReader
{
    public void writeKeyBinds(InputActionReference action)
    {
        /*
        string bindString = JsonUtility.ToJson(action.action.bindings[0].effectivePath);
        Debug.Log(bindString);
        System.IO.File.WriteAllText(Application.persistentDataPath + "/settings.json", bindString);
        Debug.Log(Application.persistentDataPath);
        */


    }
}
